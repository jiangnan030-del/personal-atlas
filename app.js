const body=document.body;const modeButton=document.querySelector('.mode-toggle');const modeLabel=document.querySelector('.mode-label');modeButton.addEventListener('click',()=>{const active=body.classList.toggle('explore');modeButton.setAttribute('aria-pressed',String(active));modeLabel.textContent=active?'快速模式':'探索模式';localStorage.setItem('atlas-mode',active?'explore':'fast')});if(localStorage.getItem('atlas-mode')==='explore'){body.classList.add('explore');modeButton.setAttribute('aria-pressed','true');modeLabel.textContent='快速模式'}
const copies=[...document.querySelectorAll('.view-copy')];function setView(view){copies.forEach(el=>{el.textContent=el.dataset[view]});document.querySelectorAll('[role=tab]').forEach(btn=>btn.setAttribute('aria-selected',String(btn.dataset.view===view)))}setView('story');document.querySelector('.evidence-switch').addEventListener('click',e=>{if(e.target.matches('button'))setView(e.target.dataset.view)});
const dialog=document.querySelector('.ask-dialog');document.querySelectorAll('.ask-open').forEach(b=>b.addEventListener('click',()=>dialog.showModal()));document.querySelector('.dialog-close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});const answers={'你擅长什么？':'我关注产品策略、AI 原生体验与数字叙事，擅长把模糊问题拆成可验证的假设，并推动它们成为清晰的产品。','最近在做什么？':'最近在推进个人网站、移动应用发布规划与营销网站重构，同时持续研究 AI 原生工作流。','如何与你合作？':'请发送项目背景、核心问题、期望时间与已有材料至 jiangnan030@gmail.com，我会尽快回复。'};document.querySelector('.quick-asks').addEventListener('click',e=>{if(e.target.matches('button'))document.querySelector('.answer').textContent=answers[e.target.textContent]});
const canvas=document.querySelector('#atlasCanvas');const ctx=canvas.getContext('2d');let px=.5,py=.5;function draw(){const w=canvas.width,h=canvas.height;ctx.clearRect(0,0,w,h);const dark=body.classList.contains('explore');ctx.strokeStyle=dark?'rgba(255,255,255,.2)':'rgba(23,24,22,.17)';ctx.fillStyle=dark?'rgba(142,185,255,.7)':'rgba(36,93,245,.55)';const center={x:w*.5+(px-.5)*30,y:h*.5+(py-.5)*30};const pts=[[.27,.2],[.79,.31],[.72,.78],[.18,.7],[.14,.46]];pts.forEach((p,i)=>{const x=w*p[0]+(px-.5)*(i-2)*9,y=h*p[1]+(py-.5)*(2-i)*8;ctx.beginPath();ctx.moveTo(center.x,center.y);ctx.lineTo(x,y);ctx.stroke();ctx.beginPath();ctx.arc(x,y,4,0,Math.PI*2);ctx.fill()});requestAnimationFrame(draw)}draw();const atlas=document.querySelector('.atlas');atlas.addEventListener('pointermove',e=>{const r=atlas.getBoundingClientRect();px=(e.clientX-r.left)/r.width;py=(e.clientY-r.top)/r.height});atlas.addEventListener('pointerleave',()=>{px=.5;py=.5});
